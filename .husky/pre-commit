# ponytail: update-index --again re-stages whole files — partial staging (git add -p) loses splits; switch to lint-staged if that bites
pnpm exec biome format --write --staged --no-errors-on-unmatched
git update-index --again
